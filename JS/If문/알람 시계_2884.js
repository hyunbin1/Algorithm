// const fs = require('fs');
// const input = fs.readFileSync("/dev/stdin").toString().split(" ");

const sample = `10 10`;
const input = sample.split(" ");

let H = Number(input[0])
let M = Number(input[1])

if(M<45;)


// 현재 시간보다 45분 빨리 알람 설정하기
// point: 분이 45분 이하일 경우 시간에서 1을 빼야된다. 

if()









